import React from 'react';
import {ScrollView, View} from "react-native";
import AppText from "../components/AppText";

function UserLocationValideScreen(props) {
    return (
        <View>
            <AppText>User location valide</AppText>
        </View>
    );
}

export default UserLocationValideScreen;